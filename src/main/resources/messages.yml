data:
  messages:
    disabled:
      "Currently disabled!"

    noItem:
      "No sellable items found!"

    # Placeholders:
    # {total.price} = money the player earned
    # {total.count} = items the player sold
    selled:
      "You selled §e {total.count} §7items for a total: §a {total.price}€"



  prefix:
    "§6§lNOTRIXMC §8• "